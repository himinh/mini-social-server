name: hello world
on:
  push:
    path:
      - '!README.md'
      - '!docs/**'
      - '**.js'
    branches:
      - 'master'

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World
      - run: echo Hello World 2
